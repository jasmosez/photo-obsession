<h1>Create New Post for <%= @user.username %></h1>

<%= form_for @post do |f| %>
  <%= f.hidden_field(:user_id, value: @user.id) %>
  <%= f.label :img_url %>
  <%= f.text_field :img_url %>
  <%= f.submit "Post!" %>
<% end %>

